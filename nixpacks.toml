providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "gcc"]
cmds = []

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && python main.py"
